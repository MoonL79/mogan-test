(define *usage-file-lines*
  (list
    "  export-buffer - Export the current buffer to another format or path"
    "  set-main-style - Set the current document's main style"
    "  set-document-language - Set the current document language"
    "  add-style-package - Add a document style package"
    "  remove-style-package - Remove a document style package"
    "  set-page-medium - Set the current document page medium"
    "  set-page-type   - Set the current document page type"
    "  set-page-orientation - Set the current document page orientation"))
